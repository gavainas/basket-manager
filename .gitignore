node_modules
dist
*.local
.DS_Store

# Salida generada por npm run design:sync (se regenera desde src/styles.css)
design/cards/

# Salida compilada por npm run sim (se regenera desde src/game)
scripts/.sim-out/

# Canvas de Claude Design ya empaquetado (2,7 MB de editor + contenido).
# Se regenera desde los .dc.html + canvas.json de al lado: ver design/canvas/LEEME.md
design/canvas/*.html
!design/canvas/*.dc.html

# Capturas sueltas de los scripts de verificación (van al scratchpad, no al repo)
/*.png
